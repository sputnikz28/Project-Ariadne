# benchmarks/random/

Baseline runs: pure random key selection, no faction logic. Used as
the statistical floor every real faction/strategy should be compared
against. No runner exists yet — structure only.
