# experiments/benchmarks/

Benchmark-related experiment sessions (working notes, ad-hoc runs)
tied to a specific research question — distinct from the top-level
`benchmarks/` folder, which holds the durable/canonical strategy
comparison results, rankings and reports once that infrastructure
exists. No sessions exist yet — structure only.
